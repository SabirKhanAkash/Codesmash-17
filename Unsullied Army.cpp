#include<bits/stdc++.h>
int main()
{
    long long int T,N,M,Q,i,j,P,X,Y;
    char str[1000],name[1000];
    scanf("%lld",&T);
    for (i = 1 ; i<=T ; i++) {
      scanf("%lld%lld",&N,&M);
    gets(str);
      for (j = 1 ; j<=M ; j++) {
        scanf("%lld%lld%lld",&i,&j,&P);
      }
      scanf("%lld",&Q);
      for (k = 1 ; k<=Q ; k++) {
        gets(name);
      }
      str1[][]
    }
    return 0;
}
